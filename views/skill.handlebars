<mom-skill-set title="{{skill.title}}" subtitle="{{skill.subtitle}}" icon="{{skill.icon}}"
               icon-description="{{skill.iconDescription}}" articles="{{articles}}"
               route="{{skill.route}}"></mom-skill-set>
